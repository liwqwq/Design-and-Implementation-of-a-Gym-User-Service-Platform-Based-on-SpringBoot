spring.application.name=GymUserServicePlatform
server.port=${PORT:8080}
spring.main.banner-mode=off
spring.jackson.serialization.write-dates-as-timestamps=false

# MySQL configuration for 基于SpringBoot的健身房用户服务平台的设计与实现
spring.datasource.url=${MYSQL_URL:jdbc:mysql://127.0.0.1:3306/fitlife_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:lnj031212}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=never
spring.datasource.hikari.initialization-fail-timeout=0
