spring:
  datasource:
    url: jdbc:mysql://${MYSQL_ADDRESS:localhost:3306}/gym_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&characterEncoding=utf8&useUnicode=true&allowPublicKeyRetrieval=true
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:lnj031212}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    serialization:
      write-dates-as-timestamps: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  security:
    user:
      name: admin
      password: admin123
      roles: ADMIN

jwt:
  secret: gym_service_secret_key
  expiration: 86400

server:
  port: 8080
